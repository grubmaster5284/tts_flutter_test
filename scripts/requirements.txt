# Python dependencies for TTS scripts
# Install with: pip3 install -r requirements.txt
# Or install globally: pip3 install --user -r requirements.txt

# OpenAI TTS
openai>=1.40.0

# Gemini TTS (uses httpx for API calls and OAuth2 for authentication)
httpx>=0.27.0
google-auth>=2.27.0
google-auth-httplib2>=0.2.0
requests>=2.32.0

